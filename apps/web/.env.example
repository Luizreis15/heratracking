# Web — copie para .env.local e preencha os valores reais
# Apenas variáveis VITE_ são expostas ao bundle — NUNCA coloque secrets aqui

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=...
